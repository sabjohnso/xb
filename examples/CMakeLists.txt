# BES examples
#
# Each subdirectory is a self-contained example demonstrating a BES
# workflow.  All examples use xb_add_library() for code generation.

add_subdirectory(bes-hello)
add_subdirectory(bes-market-data)
add_subdirectory(bes-protocol-stack)
add_subdirectory(bes-variable-length)
